// SECURE CHATBOT IMPLEMENTATION USING CHATBASE
// No API keys are stored or used in client-side code
// All AI processing is handled securely by Chatbase's servers

document.addEventListener('DOMContentLoaded', () => {
    // Chatbase is already embedded globally in index.html
    // This ensures the chatbot works securely without exposing any secrets
    console.log('Secure chatbot initialized via Chatbase third-party service');
});